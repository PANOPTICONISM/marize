import React from "react";
import StoreInfo from "../StoreInfo/StoreInfo";

function Footer() {
    return (
        <footer>
            <StoreInfo />
        </footer>
    );
}

export default Footer;
